package tipqr.back.entity.enums;

/** Cómo se reparte una propina grupal entre los miembros del grupo. */
public enum TipoDistribucion {
    EQUITATIVO,  // partes iguales
    PORCENTAJE   // cada miembro tiene un porcentaje configurado (suman 100)
}
